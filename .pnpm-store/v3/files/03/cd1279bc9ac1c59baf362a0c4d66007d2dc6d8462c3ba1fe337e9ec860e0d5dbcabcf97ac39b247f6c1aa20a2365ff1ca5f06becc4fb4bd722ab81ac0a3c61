import { Span } from "opentracing";
export declare function sourceNodesApiRunner({ traceId, deferNodeMutation, parentSpan, webhookBody, pluginName, }: {
    traceId: string;
    webhookBody: unknown;
    pluginName?: string;
    parentSpan?: Span;
    deferNodeMutation?: boolean;
}): Promise<void>;
